<Window x:Class="Chat_Group_System.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NexChat - Login" Height="550" Width="400"
        WindowStartupLocation="CenterScreen" Background="#F8F7F3" FontFamily="Segoe UI, Inter">
    <Window.Resources>
        <!-- Standard Input Style -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#F5F4F0"/>
            <Setter Property="BorderBrush" Value="#E8E7E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#F5F4F0"/>
            <Setter Property="BorderBrush" Value="#E8E7E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <!-- Action Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B3A8B"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4F4EAA"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="#FFFFFF" CornerRadius="12" Margin="30,40" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <!-- Logo / Title -->
                <TextBlock Text="🔷 NexChat" FontSize="24" FontWeight="Bold" Foreground="#3B3A8B" HorizontalAlignment="Center"/>
                <TextBlock Text="Welcome back! Please login." FontSize="14" Foreground="#6B6B6B" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <!-- Form -->
                <TextBlock Text="EMAIL" FontSize="10" FontWeight="SemiBold" Foreground="#6B6B6B" Margin="0,0,0,5"/>
                <TextBox x:Name="txtEmail" Margin="0,0,0,15"/>
                <TextBlock Text="Password" FontSize="10" FontWeight="SemiBold" Foreground="#6B6B6B" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,5">
                    <PasswordBox x:Name="txtPassword" Padding="10,0,35,0"/>
                    <TextBox x:Name="txtVisiblePassword" Visibility="Collapsed" Padding="10,0,35,0"/>
                    <ToggleButton x:Name="btnTogglePassword" HorizontalAlignment="Right" Width="30" Height="40" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnTogglePassword_Click">
                        <TextBlock Text="👁" FontSize="16" Foreground="#9A9A9A"/>
                    </ToggleButton>
                </Grid>

                <TextBlock x:Name="btnForgotPassword" Text="Forgot password?" Foreground="#6B6B6B" FontSize="12" Margin="0,0,0,25" HorizontalAlignment="Right" Cursor="Hand" MouseDown="BtnForgotPassword_MouseDown"/>

                <Button Content="Login" Style="{StaticResource PrimaryButtonStyle}" Click="BtnLogin_Click" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Don't have an account? " Foreground="#6B6B6B" FontSize="13"/>
                    <TextBlock x:Name="btnRegister" Text="Register" Foreground="#3B3A8B" FontWeight="SemiBold" FontSize="13" Cursor="Hand" MouseDown="BtnRegister_MouseDown"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>